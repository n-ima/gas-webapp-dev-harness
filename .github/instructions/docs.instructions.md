---
applyTo: "docs/**"
---

- `docs/` 配下のファイルは、対応する `*_template.md` の見出し構成を維持する。見出しを削らない。
- テンプレートファイル自体（`*_template.md`）は編集しない。コピーして実体ファイルを作る。
- 表形式の項目は空欄のまま残さず、「該当なし」等を明示する。
- Mermaid図は必ずシンタックスが有効か確認する（レンダリングエラーを残さない）。
