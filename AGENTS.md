# AGENTS.md — このリポジトリで動くすべてのAIエージェント向け共通指示

このファイルは、このハーネスで動くすべてのAIエージェントの**唯一の正**となる
指示ファイルです（`.github/copilot-instructions.md` と重複させず一本化）。
GitHub Copilot と Antigravity は本ファイルを直接読み、Claude Code は
`CLAUDE.md`（`@AGENTS.md` インポート）経由で読みます。

## プラットフォーム対応（Copilot / Claude Code / Antigravity）

このハーネスは3つのエージェント環境で同じ振る舞いになるよう、
**正のレイヤ + 薄いアダプタ**の構成をとる。

- **正（振る舞いの定義）**: 本ファイル、`.github/agents/*.agent.md`（役割定義）、
  `.github/prompts/*.prompt.md`（起動指示）、`.github/skills/*/SKILL.md`（手順）。
  **振る舞いの変更は必ずこの層だけで行う。**
- **アダプタ（プラットフォーム固有の入口。ポインタのみで振る舞いを持たない）**:
  - Claude Code: `CLAUDE.md`（本ファイルをインポート）、`.claude/commands/`
    （スラッシュコマンド→正を参照）、`.claude/agents/`（サブエージェント→正を参照）、
    `.claude/skills/`（スキルポインタ。Claude Codeは`.claude/skills/`しか探索しないため）、
    `.claude/settings.json`（同じフックスクリプトをClaude Codeスキーマで配線）
  - Antigravity: 本ファイルを直接読む + `.agents/workflows/`（/コマンド→正を参照）
- **読み替え規則**: `runSubagent` は Claude Code では Task ツール（`.claude/agents/` の
  同名サブエージェント）、Antigravity では Agent Manager の別エージェント会話。
  ハンドオフボタンは Copilot 専用（他環境では「新しいセッションで /コマンド実行」の案内に統一）。
- **アダプタにはプロンプト固有の手順を書かない**（起動経路の等価性ルールの
  プラットフォーム拡張）。アダプタを直したくなったら、それは正の層を直すべきサイン。
  正のスキルを新設したら `.claude/skills/` のポインタも同時に作る（skill-authoring参照）。
- **ガードレールの強度差（機能別の劣化モード）**:
  - Copilot: フック（`.github/hooks/`、Preview）で deny/ask を機械的に強制。
  - Claude Code: 同じフックスクリプト（`.claude/settings.json` の hooks）に加えて
    `permissions.deny`（ツールレベルのハードブロック）の**二重の機械的ガード**。
    3環境で最も強い。
  - Antigravity: **IDE(GUI)はプロジェクト内のスクリプトフックを読まない**
    （姉妹プロジェクトai-managerでの実機検証により判明した知見）。機械的な保護は
    IDEの **Deny List（Settings → Permissions → Advanced）** に危険コマンド
    （`git push --force`, `rm -rf` 等）を登録して代替し、それ以外は指示レベルの遵守＋
    Git側の保護（ブランチ保護/CODEOWNERS）に依存する。重要なリリース判断は
    フックが効く環境（Copilot/Claude Code）で行うことを推奨。

## このリポジトリについて

特定のアプリのコードではなく、「要件定義 → 設計 → 実装 → テスト → リリース」を
一気通貫でオーケストレーションするための **汎用開発ハーネス（雛形テンプレート）** です。
新しいアプリを作るときはこの構成をコピーし、`requirements/memo.md` に要件メモを
置くところから始めます。

## フェーズとゲート：フェーズごとに「人の関わり方」が違う

1. **要件定義**（`docs/01-requirements/`）— **人が最も関わるフェーズ**。
   質問と対話を通じて厚くヒアリングする。特にデプロイ環境・自動化してよい範囲
   （`environment.md`）は仮置きせず具体的に聞き切る。ここが曖昧だと後工程がすべて破綻する。
   完了には明示的なゲート承認が必要。
2. **設計**（`docs/02-design/`）— **エージェントが決め、人は質問への回答と最終承認だけ**。
   これは要件定義フェーズで十分に詳細な情報（機能要件・非機能要件・環境情報）が
   揃っていることが前提であり、要件が薄いまま設計を丸投げしてはいけない。
   設計エージェントは着手前に要件の充足度を自分で確認し、不足があれば要件定義に差し戻す。
   アーキテクチャ・詳細設計はエージェントが自分で決めて完成させる。後戻りが困難で
   ビジネス上の影響が大きい選択（技術スタック等）だけユーザーに選んでもらう。
   完了には最終承認が必要（項目ごとの逐一確認はしない）。
3. **実装**（`docs/03-implementation/`）・**4. テスト**（`docs/04-test/`）—
   **全自動区間**。設計の最終承認後、人の逐一確認なしでノンストップに進む。
   人が関わるのは、真のブロッカー（設計・要件だけでは判断できない事実が必要なとき）と、
   `.github/hooks/` が機械的に確認を入れる破壊的操作のときだけ。
   テスト完了後・リリース前には、実装した本人とは別コンテキストの `reviewer` サブエージェントが
   独立レビュー（正しさ・セキュリティ・品質）を行い、問題があれば実装に差し戻す。
5. **リリース**（`docs/05-release/`）— **environment.mdに基づく自動実行**。
   自動化してよいと明示された作業は自動で行い、人手必須と明示された作業（支払い、
   外部ダッシュボードでの手動承認、ドメイン購入等）だけユーザーに委ねる。
   デプロイ環境がハーネス側に未対応なら、`.github/skills/skill-authoring/SKILL.md` で
   その場で環境固有Skill（`.github/skills/deploy-<environment>/SKILL.md`）を作り、
   次回以降のリリースも自動化されるようにする。

- 各フェーズはこの順に進める。フェーズを飛ばさない。
- 成果物は必ず `docs/` 配下に Markdown で残す。会話内だけで完結させない。
- 現在のゲート状態は `docs/00-overview/progress.md` の `GATE_STATUS` ブロックが正
  （判定手順は `.github/skills/gate-check/SKILL.md` を参照）。

## エージェント構成（VS Code Copilot）

- `.github/agents/*.agent.md` — フェーズごとの専属エージェント（旧chatmode）。
  `orchestrator` → `requirements` → `design` → `implement` → `test` → `release` の順で
  `handoffs` により引き継ぐ。
- `.github/agents/reviewer.agent.md` — **サブエージェント**。`test`エージェントが
  `runSubagent` で呼び出す、実装とは独立したコンテキストのレビュー担当（読み取り専用）。
  「実装した本人がそのままレビューして自己承認する」ことを防ぐための別セッションレビュー。
- `.github/agents/task-worker.agent.md` — **サブエージェント**。`implement`エージェントが
  タスクを1つ実装するたびに `runSubagent` で呼び出す。実装ループ全体を1つの長い会話に
  しないための、コンテキストロット対策（詳細は「コンテキスト管理」節）。
- `.github/agents/spec-critic.agent.md` — **サブエージェント**。`requirements`/`design`
  エージェントがゲート承認を求める前に `runSubagent` で呼び出す、要件定義書・設計書の
  独立レビュー担当（読み取り専用）。上流の欠陥は下流ほど修正コストが大きいため、
  コードレビューより先にここへ独立レビューを置いている。
- `.github/skills/*/SKILL.md` — 各フェーズで使う手順・チェックリストの部品
  （要件引き出し、ADR作成、テストケース設計、ゲート判定、Skill作成、セキュリティレビュー）。
  `deploy-<environment>` や `stack-conventions` はプロジェクトごとに実際の環境・スタックが
  判明した時点で設計/リリースエージェントが動的に追加していく
  （ハーネスに全環境・全スタックを事前収録はしない。既存の公式/コミュニティSkillの
  流用を優先し、ゼロから書くのは最後の手段）。
- `.github/prompts/*.prompt.md` — フェーズを進めるためのスラッシュコマンド。各プロンプトは
  frontmatterの `agent:` で対応する専属エージェントに明示的にバインドしてあるため、
  現在どのエージェントを選択していても、そのプロンプトを実行した瞬間に正しいツール制限・
  振る舞いへ切り替わる（`agent:` を指定しないと、選択中の別のエージェント/既定モードの
  ツール制限のまま実行されてしまうため、意図した権限で動かない可能性がある）。
  **等価性ルール**: エージェントの起動経路は (1)プロンプト実行 (2)ハンドオフボタン
  (3)runSubagent の3つあり、**ハンドオフ経由では `.prompt.md` は読み込まれない**。
  そのため**振る舞いの正は常に `.agent.md`（またはスキル）に置き、プロンプトには
  薄い起動指示（どのフェーズ・どの段階を実行するか）だけを書く**。改修時にプロンプトへ
  手順を足したくなったら、それは `.agent.md` かスキルに書くべき内容である。
- `.github/hooks/` — フェーズゲート・セキュリティを補強する機械的なフック
  （テンプレ直接編集のdeny、push/tag等のask確認、進捗の自動注入、ハーネス自体の設定への
  無断編集のdeny、シークレットらしき文字列のdeny/ask）。Preview機能のため過信せず、
  本ファイルの指示レベルのルールと併用する。

## ナビゲーション責務（全エージェント共通のメタガイダンス）

操作手順を人が暗記する前提はスケールしない。エージェント自身が「次の一手」を案内する。

- 応答の最後に、次の一手と「新しいチャットにすべきか・このまま続けてよいか」を
  1行で案内する（USAGE.mdのセッション分割表に従う）。
- **案内は必ず実行可能なコマンド形式で書く**:
  「新しいチャットで `/03-design-architecture` を実行してください」のように、
  プロンプト名をそのまま示す。**「◯◯エージェントに切り替えてください」という案内はしない**
  （プロンプトは対応エージェントに自動バインドされるため切り替えは不要であり、
  エージェント切り替えだけではプロンプトの起動指示が実行されない）。
- ハンドオフボタンは**同一チャットで継続する場合の手段**。セッション分割表が
  「新規チャット」を推奨するフェーズ移行（要件→設計、設計→実装 等）では、
  第一の案内をプロンプトコマンドにし、ハンドオフは「小規模で会話が短いときは
  このボタンで続行も可」という補足に留める。
- 現在のフェーズ・エージェントに合わない依頼を受けたら、作業を始める前に
  正しい入口（該当するプロンプト/エージェント）を案内する。
- 完了タスク10個超・差し戻し2往復超・会話の劣化を感じたら、区切りのよいところで
  新チャットへの切り替えを自分から提案する（`docs/` が正の状態なので
  何も失われない旨を添える）。
- コストに影響する選択（強いモデルへの切り替え・並列レビュー）は理由つきで提案し、
  勝手に増やさない。
- ハーネスの使い方そのものの質問には `/98-harness-help`
  （`.github/skills/harness-guide/SKILL.md`）で答える。

## サブエージェント活用方針

- 各エージェントの `agents` frontmatterで、呼び出せるサブエージェントを明示的に絞る
  （最小権限。既定は `[]` = サブエージェント呼び出し不可）。
- サブエージェントは2つの目的で使う。
  1. **メインの会話コンテキストを汚さずに済ませたい調査・レビュー**（例: `test → reviewer`）。
  2. **繰り返しの多い作業を、毎回まっさらなコンテキストで実行してコンテキストロットを防ぐ**
     （例: `implement → task-worker`。1タスクにつき1回のサブエージェント呼び出しとし、
     実装の詳細をコーディネーター自身の会話に蓄積させない）。
- 既定で使う経路は次の4つのみ。
  - `requirements → spec-critic`（ゲート承認前に1回）: 要件定義書の抜け・曖昧さの独立レビュー
  - `design → spec-critic`（ゲート承認前に1回）: 設計書のトレーサビリティ・実装可能性の独立レビュー
  - `implement → task-worker`（タスク数だけ）: コンテキストロット対策の実装分離
  - `test → reviewer`（1回）: コードの正しさ・セキュリティの独立レビュー
  並列の多視点レビューのような「精度を上げるための追加呼び出し」は、
  プロジェクトの重要度に応じてユーザーが明示的に要求した場合のみ増やす
  （上記4経路とは異なり、コストに対して効果が見合うかの判断が要る）。
  上流（要件・設計）の欠陥ほど下流での修正コストが大きいため、
  spec-critic の2回は「最も費用対効果の高いレビュー」として既定に含めている。

## セキュリティガードレール

- 各エージェントの `tools` は必要最小限にする（`requirements`/`design` に `execute` を
  持たせない等）。
- ハーネス自体の設定ファイル（`.github/agents/`, `.github/hooks/`, `.github/workflows/`,
  `AGENTS.md`, `plugin.json`, `.vscode/settings.json`）は、プロンプトインジェクション等による
  自己権限昇格・ガードレール解除を防ぐため、エージェントが自動編集しない
  （`.github/hooks/security-hooks.json` で機械的にdeny）。変更が必要な場合は人間が直接編集する。
  ブランチ保護＋`.github/CODEOWNERS` の組み合わせも推奨する。
- **MCPサーバーのツール呼び出しは `.github/hooks/` のコマンドパターン検査の対象外**
  （Hooksはネイティブなコマンド文字列しか検査できない）。MCPに与える権限・認証情報は
  最小にし、破壊的操作が可能なMCPサーバーを既定で接続しない。
- 認証情報・APIキー・秘密鍵はコード/ドキュメントに直接書かない。保管場所
  （`environment.md` に記載）への参照のみとする。高確度パターンは
  `.github/hooks/security-hooks.json` がdeny、汎用パターンはaskで確認する。
- テスト完了後・リリース前に `reviewer` サブエージェント（`.github/skills/security-review/SKILL.md`）
  による独立したセキュリティレビューを必ず経由する。
- リリースフェーズの `git push`/タグ付け/デプロイ実行など外部に影響する操作は、
  `environment.md` の自動化可否分類に関わらず、常にユーザーの最終確認を経由する。

## コンテキスト管理（コンテキストロット対策）

長い会話ほどモデルの想起精度が落ちる現象（"context rot"）がAnthropicの検証で報告されている
（100万トークンの窓を持つモデルでも、高精度で使えるのは実質的に最初の数十万トークン程度）。
1つの長いセッションで全フェーズ・全タスクを進めようとすると、後半になるほど精度が落ちる
リスクが上がる。「1タスク1セッション」という経験則はこの現象に対する合理的な対策である。

このハーネスでは次の3つの手段でこれに対処する。

1. **ドキュメントが記憶であり、会話が記憶ではない。** `docs/` の各ファイル
   （`requirements.md`, `architecture.md`, `tasks.md`, `test-report.md`,
   `progress.md` の `GATE_STATUS` 等）がプロジェクトの正の状態である。
   会話を新しく始めても、これらのファイルを読めば同じ場所から再開できる設計にしてある。
   フェーズが変わるタイミング（要件定義→設計、設計→実装 等）や、1つのフェーズ内でも
   会話が長くなってきたと感じたときは、**会話を続けるより新しいチャットセッションを
   始めて関連ドキュメントを読み直す方を優先してよい**。何かを失うことはない。
   具体的な分割基準（フェーズ別の表と、継続中でも切り替える4条件）は USAGE.md の
   セッション分割表を正とする。
2. **サブエージェントによる実装ループの分離。** `implement` エージェントは、
   タスクの実装そのものを `task-worker` サブエージェントに1タスクずつ委譲する。
   これにより、タスク数がいくつであっても個々の実装は毎回まっさらなコンテキストで行われ、
   コーディネーター自身の会話は「進捗管理」という軽い情報だけに保たれる。
3. **独立レビューも同じ仕組みで実現している。** `reviewer` サブエージェントが
   実装時の思考過程を引き継がずに評価できるのも、これと同じ「フレッシュなコンテキスト」の
   効果による（バイアス除去とコンテキストロット対策を兼ねる）。

## コスト（トークン/AI Credit）についての方針

GitHub Copilotはトークン量に応じた従量課金であり、モデル選択とコンテキスト量が
コストに直結する。このハーネスでは以下を既定にしている。

- 呼び出し頻度が高く機械的な判断が中心のエージェント（`orchestrator`, `implement`, `test`）
  は `model: auto`（利用可能な中で最も安価に処理できるモデルを自動選択）を既定にする。
- 一方で、後工程すべてが依存する重要な判断（`design` のアーキテクチャ決定、
  `reviewer` の正しさ/セキュリティ判定）は、間違えたときの手戻りコストの方が
  モデル代より高くつくため、ユーザーが利用可能な範囲で強いモデルへの切り替えを検討してよい。
- Agent Skillsは段階的開示（discovery時は`name`/`description`のみ読み込み、関連する時だけ
  本文を読み込む）でコンテキスト消費を抑える設計になっている。Skillの`description`は
  具体的でキーワードが立つように書く（曖昧だと無関係な場面で読み込まれて浪費するか、
  必要な場面で読み込まれず機能しない）。
- サブエージェントは文脈を分離できる一方、呼び出しごとに追加のモデル呼び出しが発生する。
  「メインの会話を汚さないため」に使うのであって、「並列で多く呼べば精度が上がるから」を
  既定の理由にしない（上記のサブエージェント活用方針を参照）。
- Hooksはモデルを介さないシェル実行であり、ほぼゼロコストでルールを強制できる。
  「モデルに繰り返し指示して守らせる」より「Hooksで機械的に強制する」方が
  コストと確実性の両面で優れる場面では、指示ではなくHooksを増やすことを検討する。

## 差分駆動の原則（リリース後・承認後の修正）

各エージェントは会話の記憶ではなく `docs/` のファイルを毎回読み直す設計にしているため、
文書を後から修正して続行すること自体は安全である。ただし修正の性質によって
必要な手続きが異なる。**全フェーズのやり直しは決してしない**（変更部分だけ辿る）。

1. **軽微な誤記**（表記ゆれ・数値訂正・他に波及しない修正）→ 該当文書を直接修正して続行。
2. **影響のある要件・設計変更**（スコープ・決定の変更）→ 変更部分について
   **該当フェーズだけ再ゲート**する。影響範囲はトレーサビリティ表から辿る。
   エージェントに任せると影響範囲の洗い出し・トレーサビリティ更新・関連ADR見直しまで
   行われる（手動編集だとこれが漏れる）。
3. **設計変更を伴わないバグ修正**（実装が設計とずれていた）→ 文書修正・再ゲート不要。
   再現テスト先行で最小修正する。
4. **緊急ホットフィックス** → 許容する。ただし再現テスト・PR・人の承認・記録は省略しない。
   修正と同時に該当文書へ「実装乖離あり（解消期限: 日付）」を注記し、
   `learnings.md` に1行追記する（SessionStart注入に乗るため忘れられなくなる）。
   期限までに文書を更新して整合を回復する。
   **黙って乖離したままにすることだけが禁止**（緊急対応を禁止すると障害時に必ず破られ、
   破られたルールは記録すらされない。だから「記録すれば許容」に倒す）。

どの場合でも、ゲート承認済み（`GATE_STATUS` が `done`）の文書を編集すると
`warn-stale-gate` フックが整合確認の警告を自動で出す。これは気づきの補助であり、
実際の確認は人（またはエージェントへの指示）で行う。
改修サイクルの具体的な手順（どのフェーズをin_progressに戻すか）は
`.github/skills/gate-check/SKILL.md` を参照。

## 成長ループ（このハーネスは使うたびに賢くなる設計）

記録の3層構造で「使った経験」を次の入力に変える。詳細は
`.github/skills/harness-retrospective/SKILL.md`。

1. **教訓（`docs/00-overview/learnings.md`）** — 全エージェント共通のルール:
   次のいずれかに該当したら、その場で1行追記する（`- [YYYY-MM-DD] 教訓`）。
   - ユーザーに同じ趣旨の訂正を受けたとき
   - 同じアプローチで2回失敗したとき
   - プロジェクト固有の暗黙知が判明したとき
   - **コマンド・ツールの実行方法を試行錯誤の末に確立したとき**（2回以上失敗してから
     成功した実行手順・回避策は、**成功したコマンドをそのまま**記録する。
     仕様はdocsに残るが実行方法の獲得知識は書かないと消え、次のセッションが
     同じ試行錯誤を繰り返す最大の原因になる。仕様の教訓より優先して書く）
   - **セッションを終える・別フェーズへ引き継ぐ前に**、このセッションで確立した
     環境固有の実行方法が記録済みかを確認する（未記録のまま次へ渡さない）
   このファイルはSessionStartフックで以後のすべてのセッションに自動注入されるため、
   書けば確実に効く。一般的なベストプラクティスはここではなくスキルに書く。
2. **振り返り（`docs/06-retrospective/`）** — リリース後（または任意のタイミング）に
   `/10-retrospective` で実施。摩擦のあった出来事を
   「ハーネス改善 / プロジェクト固有 / 一過性」に分類し、ハーネス改善分は
   対象ファイル・問題・提案・根拠の4点セットの改善提案表にする。
3. **本体への還流** — 改善提案と再利用可能なSkill（`deploy-*` 等）は、
   ハーネス本体リポジトリに人間が適用する（ハーネス設定は自動編集禁止のため）。
   適用時は本体の `DECISIONS.md` に根拠つきで記録する。
   これにより、以後の新プロジェクトは改善済みの状態から始まる。

## 破壊的操作・外部影響のあるアクション

- `git push`、タグ付け、force系操作、外部サービスへの通知など、影響が大きい・
  後戻りしにくい操作は必ず事前にユーザーに確認してから実行する
  （`.github/hooks/` によっても機械的に確認が入るようにしているが、フックが
  発火しない環境でも必ず確認すること）。
- リリースフェーズであっても、実際のリリース作業（タグ付け・公開）はユーザーの
  明示的な承認を得てから行う。

## コーディング規約（実装フェーズ共通）

- 要件にない機能・抽象化を先回りして作らない（YAGNI）。
- コメントは「なぜ」を書くときだけ。「何をしているか」は書かない。
- セキュリティ（OWASP Top10相当）に配慮し、入力値検証はシステム境界で行う。
- テストは `docs/04-test/` のテストケースに対応づけて書く。

## ドキュメント規約

- `docs/` 配下の実体ファイルは対応する `*_template.md` の見出し構成を維持する。
- `*_template.md` 自体は編集しない（コピーして実体ファイルを作る）。
- 表形式の項目は空欄のまま残さず、「該当なし」等を明示する。

## 参照

- 使い方全体: [README.md](README.md)
- 使い方の具体例: [USAGE.md](USAGE.md)
- **このハーネス自体の設計判断の根拠・経緯**: [DECISIONS.md](DECISIONS.md)
  （ハーネスを改修する前に必ず目を通す。同じ議論の繰り返しや、直したバグの再導入を防ぐため）
- エージェント一覧: [.github/agents/](.github/agents/)
- スキル一覧: [.github/skills/](.github/skills/)
- プロンプト一覧: [.github/prompts/](.github/prompts/)
